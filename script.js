"use strict";
var hello = "Hello";
var world = "World";
console.log(hello + " " + world);
// After adding this file we run the command in terminal
// Command is tsc <filename.ts>
// This will compile <filename.ts> to <filename.js>
// which we can seen working in browser
